[tool.poetry]
name = "prueba-gpt"
version = "0.1.0"
description = "Sandbox con calculadora, juego y algoritmos QUBO/QUDO."
authors = ["Sergio"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
numpy = "^1.26.4"
scipy = "^1.15.3"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
